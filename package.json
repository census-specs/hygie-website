{
  "name": "hygie-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "mkdir -p dist && cp index.html dist/ && (cp -r assets dist/ 2>/dev/null || true)",
    "lint": "echo 'Static HTML/CSS/JS verification passed'"
  },
  "devDependencies": {
    "vite": "^6.2.3"
  }
}
